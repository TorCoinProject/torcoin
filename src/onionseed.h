#ifndef TORCOIN_ONIONSEED_H
#define TORCOIN_ONIONSEED_H

// hidden service seeds
static const char *strMainNetOnionSeed[][1] = {
    {NULL},
    {NULL}
};

static const char *strTestNetOnionSeed[][1] = {
    {NULL}
};

#endif